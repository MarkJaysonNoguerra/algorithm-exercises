type Point = {
  x: number;
  y: number;
};

export const mazeSolver = (
  maze: string[][],
  wall: string,
  startPoint: Point,
  endPoint: Point
): Point[] => {
  const solvePath: Point[] = [];
  return solvePath;
};
